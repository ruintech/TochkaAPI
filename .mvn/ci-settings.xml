<?xml version="1.0" encoding="UTF-8"?>
<!--
  Настройки Maven для CI. Учётные данные не хранятся в файле: они подставляются из переменных
  окружения NEXUS_USERNAME и NEXUS_PASSWORD, которые задаются секретами репозитория.

  Использование: ./mvnw -s .mvn/ci-settings.xml deploy
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              https://maven.apache.org/xsd/settings-1.0.0.xsd">
  <servers>
    <server>
      <id>nexus-releases</id>
      <username>${env.NEXUS_USERNAME}</username>
      <password>${env.NEXUS_PASSWORD}</password>
    </server>
    <server>
      <id>nexus-snapshots</id>
      <username>${env.NEXUS_USERNAME}</username>
      <password>${env.NEXUS_PASSWORD}</password>
    </server>
  </servers>
</settings>
